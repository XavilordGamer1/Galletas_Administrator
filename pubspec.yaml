name: galletas_app_nueva
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Dependencias base
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  barcode_widget: ^2.0.4
  path_provider: ^2.1.3
  csv: ^6.0.0
  gallery_saver: ^2.3.2

  # FORZANDO LA ACTUALIZACIÓN A LAS ÚLTIMAS VERSIONES
  mobile_scanner: ^7.0.1
  permission_handler: ^12.0.1
  share_plus: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0 # Usando la última versión

flutter:
  uses-material-design: true